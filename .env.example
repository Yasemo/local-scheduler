# Local Scheduler Service Configuration
# Copy this file to .env and adjust values for your environment

# How often to trigger the scheduler (in milliseconds)
# Default: 30000 (30 seconds)
SCHEDULER_INTERVAL=30000

# Secret for authenticating with the scheduler endpoint
# This should match the SCHEDULER_SECRET in your main app
# Generate a secure random string for production
SCHEDULER_SECRET=your-secret-key-here-change-me

# Base URL of your application
# Default: http://localhost
APP_URL=http://localhost

# Optional: Port number if not using default (80)
# Uncomment and set if your app runs on a different port
# APP_PORT=3000
